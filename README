Runs nethogs inside a docker container. Nethogs won't be able to provide you with any useful information such as what process is using how much bandwith. It will only show you the current network usage for the given interface. 
Run it: `docker run -it --net=host --rm matt3o12/nethogs`
